# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration (Comma separated list of allowed origins)
# For mobile apps, keep capacitor://localhost in the list
CORS_ORIGINS=https://compareit.app,capacitor://localhost,http://localhost:5173
